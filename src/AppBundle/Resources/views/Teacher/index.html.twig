{% extends "AppBundle:Teacher:layout.html.twig" %}

{% block content -%}
    <h1><span class="glyphicon glyphicon-briefcase" aria-hidden=""true"></span> Teacher List</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th width='10%'>Show</th>
                <th width='10%'>Edit</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.firstName }}</td>
                <td>{{ entity.lastName }}</td>
                <td width="10%">
                    <a href="{{ path('teacher_show', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-eye-open" aria-hidden=""true"></span></a>
                </td>
                <td width="10%">
                    <a href="{{ path('teacher_edit', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-edit" aria-hidden=""true"></span></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
{%block column_list%}active{%endblock%}
{%block column_add%}{%endblock%}